set $mod Mod4
floating_modifier $mod
set $TEMINAL alacritty

#######################
#     Appearance      #
#######################

# Font
font pango: SF Pro Text 11

# Colors
set $bg-color           #282c34
set $inactive-bg-color  #282c34
set $t-color            #ffffff
set $inactive-t-color   #5c6370
set $urgent-bg-color    #be5046
set $focus-border       #1f4863
set $bg-lighter         #323640

# Window colors
#                         border               background           text              indicator
#                         --------------------------------------------------------------------
client.focused            $focus-border        $bg-color            $t-color          00633c
client.unfocused	        $inactive-bg-color	 $inactive-bg-color   $inactive-t-color	00633c
client.focused_inactive	  $inactive-bg-color	 $inactive-bg-color   $inactive-t-color	00633c
client.urgent		          $urgent-bg-color     $urgent-bg-color     $t-color          00633c

# Hide indicator
hide_edge_borders both

# i3-bar

bar {
	i3bar_command i3bar
  position top
	status_command i3blocks -c /home/unnat/.config/i3blocks/config
	workspace_min_width 38px
	strip_workspace_numbers true
	colors {
		background $bg-color
		#                   border            background        text
		#                   -----------------------------------------------------
		focused_workspace   $bg-lighter       $bg-lighter       $t-color
		inactive_workspace  $bg-lighter       $bg-lighter       $inactive-t-color	
		urgent_workspace    $urgent-bg-color  $urgent-bg-color  $t-color
	}	
}


#######################
#     Workspaces      #
#######################

set $ws1 "1: "
set $ws2 "2: "
set $ws3 "3: "
set $ws4 "4: "
set $ws5 "5: "
set $ws6 "6: "
set $ws7 "7: "
set $ws8 "8: Other"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8

# Assign specific workspaces to several windows
assign [class="Code"] $ws2
assign [class="kdenlive"] $ws6
assign [class="zoom"] $ws7

##################
#     Modes      #
##################

# Resize Mode
mode "resize" {
	bindsym Left resize shrink width 2 px or 2 ppt
	bindsym Down resize grow height 2 px or 2 ppt
	bindsym Up resize shrink height 2 px or 2 ppt
	bindsym Right resize grow width 2 px or 2 ppt
	
	# Exit
	bindsym Return mode "default"
	bindsym Escape mode "default"
	bindsym $mod+r mode "default"
}


# Mouse Mode
mode "mouse" {

	# Setting speed
	set $slow 8
	set $quick 14
	set $fast 50

	# Mouse movement (slow/fine-tuned)
	bindsym h exec xdotool mousemove_relative -- -$slow 0
	bindsym j exec xdotool mousemove_relative 0 $slow
	bindsym k exec xdotool mousemove_relative -- 0 -$slow
	bindsym l exec xdotool mousemove_relative $slow 0

	# Mouse movement (quicker) 
	bindsym Shift+h exec xdotool mousemove_relative -- -$quick 0
	bindsym Shift+j exec xdotool mousemove_relative 0 $quick
	bindsym Shift+k exec xdotool mousemove_relative -- 0 -$quick
	bindsym Shift+l exec xdotool mousemove_relative $quick 0

	# Mouse movement (fast)
	bindsym Shift+$mod2+h exec xdotool mousemove_relative -- -$fast 0
	bindsym Shift+$mod2+j exec xdotool mousemove_relative 0 $fast
	bindsym Shift+$mod2+k exec xdotool mousemove_relative -- 0 -$fast
	bindsym Shift+$mod2+l exec xdotool mousemove_relative $fast 0

	# Left Click
	bindsym f exec xdotool click 1
	# Middle Click
	bindsym s exec xdotool click 2
	# Right Click
	bindsym d exec xdotool click 3
	# Left Click-and-drag
	bindsym Shift+f exec xdotool mousedown 1
	# Middle Click-and-drag 
	bindsym Shift+s exec xdotool mousedown 2
	# Right Click-and-drag
	bindsym Shift+d exec xdotool mousedown 3
	# Left Drag release
	bindsym $mod2+f exec xdotool mouseup 1
	# Middle Drag release
	bindsym $mod2+d exec xdotool mouseup 2
	# Right Drag release
	bindsym $mod2+s exec xdotool mouseup 3

	# Exit
	bindsym Return mode "default"
	bindsym Escape mode "default"
	bindsym $mod+g mode "default"
}

###########################
#    Startup Programs     #
###########################

exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec parcellite
exec emote
exec numlockx
exec setxkbmap -option ctrl:nocaps
exec_always picom --experimental-backend
exec xmodmap ~/.config/.Xmodmap

# Background Image
exec feh --bg-fill '/home/unnat/Pictures/backgrounds/pexels-zhaocan-li-1755243.jpg'


#######################
#     Keybindings     #
#######################

bindsym $mod+f fullscreen toggle global
bindsym Print exec xfce4-screenshooter -r -m -s /home/unnat/Pictures/Screenshots/
bindsym Shift+Print exec xfce4-screenshooter -f -m -s /home/unnat/Pictures/Screenshots/
bindsym $mod+Tab workspace next
bindsym $mod+Shift+Tab workspace back_and_forth 
bindsym $mod+Mod1+Left exec xrandr --output VGA1 --rotate left
bindsym $mod+Mod1+Right exec xrandr --output VGA1 --rotate right
bindsym $mod+Mod1+Down exec xrandr --output VGA1 --rotate inverted
bindsym $mod+Mod1+Up exec xrandr --output VGA1 --rotate normal
#bindsym $mod+semicolon exec emote
bindsym $mod+l exec i3lock -i ~/Pictures/backgrounds/arch-lockscreen.png
bindsym $mod+d exec $HOME/.config/rofi/bin/launcher_colorful
bindsym $mod+Shift+e exec $HOME/.config/rofi/bin/powermenu
bindsym $mod+Return exec $TEMINAL
bindsym $mod+Shift+q kill
bindsym ctrl+Mod1+x xkill
bindsym $mod+h split h
bindsym $mod+v split v
bindsym $mod+Shift+space floating toggle
bindsym $mod+space focus mode_toggle
bindsym $mod+a focus parent
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart

# Trigger Modes
bindsym $mod+g mode "mouse"
bindsym $mod+r mode "resize"

# Change focused window
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Layouts
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# Move focused window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Volume buttons to set volume
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl -- set-sink-volume 0 +10% ; exec pkill -SIGRTMIN+10 i3blocks
bindsym XF86AudioLowerVolume exec --no-startup-id pactl -- set-sink-volume 0 -10% ; exec pkill -SIGRTMIN+10 i3blocks
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle ; exec pkill -SIGRTMIN+10 i3blocks
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute 0 toggle ; exec pkill -SIGRTMIN+10 i3blocks

###########################
#     Windows config      #
###########################

# Disable borders for some windows
for_window [class="firefox"] border pixel 2 

# Padding in Title bar
for_window [class=".*"] title_format "<span size='smaller'>  %title  </span>"

# Windows to be in floating layout by default
for_window [class="Pavucontrol"] floating enable
for_window [class="Nm-connection-editor"] floating enable
for_window [class="Lxappearance"] floating enable
for_window [class="Arandr"] floating enable

# Disable focus on avtivation
focus_on_window_activation none

# Disable mouse hover focus
focus_follows_mouse no 

# i3 gaps
smart_gaps on
gaps inner 3
for_window [class=".*"] border pixel 0

